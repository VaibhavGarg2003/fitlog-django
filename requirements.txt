# Production dependencies — pinned by pip freeze discipline at deploy time;
# ranges here express intent, CI installs and tests exactly what ships.
Django>=5.2,<6.0
djangorestframework>=3.15
django-cors-headers>=4.4
django-environ>=0.11
PyJWT[crypto]>=2.9
psycopg[binary]>=3.2
gunicorn>=23.0
whitenoise>=6.7
